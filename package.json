{
  "name": "listing-refresh",
  "version": "0.1.0",
  "description": "Etsy/print-on-demand listing refresher — paste a listing, get a rewritten title, tags and opening line, plus a weekly queue of the 5 stalest listings to touch.",
  "private": true,
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}
