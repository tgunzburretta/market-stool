{
  "name": "cleanproof",
  "version": "0.1.0",
  "description": "Turnaround proof for short-let cleaners — per-property checklists, before/after photos, and timestamped PDF reports sent to hosts on completion.",
  "private": true,
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "dev": "node --watch server/index.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "multer": "^2.0.1",
    "nodemailer": "^9.1.1",
    "pdfkit": "^0.15.0",
    "uuid": "^14.0.2"
  }
}
